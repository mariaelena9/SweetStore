import { FilterListTPipe } from './filter-list-t.pipe';

describe('FilterListTPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterListTPipe();
    expect(pipe).toBeTruthy();
  });
});
